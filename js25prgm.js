function greaterNum(a,b) {
    if(a>b)
    console.log("The greatest number of "+a+" and "+b+" is "+a);
    
    else
    console.log("The greatest number of "+a+" and "+b+" is "+a);
}
greaterNum(10,5);
greaterNum(16,9);
